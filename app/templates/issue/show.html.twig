{% extends 'base.html.twig' %}

{% block title %}Issue{% endblock %}

{% block body %}
    <h1>Issue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ issue.id }}</td>
            </tr>
            <tr>
                <th>FIELDS:</th>
            </tr>
            {% for field in issue.issueFieldValues %}
                {% dump(field) %}
                <tr>
                    <th>{{ field.issueFiled }}</th>
                    <td>
{#                        {% if field.getRouteValue %}#}
{#                            <a href="{{ path(field.getRouteValue,field.getValue) }}">{{ field.getValueForTemplate }}</a>#}
{#                        {% else %}#}
                            {{ field.getValueForTemplate }}
{#                        {% endif %}#}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_issue_index') }}">back to list</a>

    <a href="{{ path('app_issue_edit', {'id': issue.id}) }}">edit</a>

    {{ include('issue/_delete_form.html.twig') }}
{% endblock %}
