<form method="post" action="{{ path('app_issue_field_delete', {'id': issue_field.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ issue_field.id) }}">
    <button class="btn">Delete</button>
</form>
